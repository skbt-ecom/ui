if (process.env.NODE_ENV !== "production") {
  console.error(
    '⚠️ Deprecation component "TextField". Use direct import @material-ui/core/TextField instead'
  )
}
/**
 * @deprecated use direct import &#64;material-ui/core/TextField instead
 */
export { default } from "@material-ui/core/TextField"
