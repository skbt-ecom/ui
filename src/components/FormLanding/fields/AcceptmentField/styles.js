import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  personalLabel: {
    fontWeight: 300,
    fontSize: 10,
    lineHeight: 1.2,
  },
  helperTextRoot: {
    marginTop: -10,
  },
});

export default useStyles;
