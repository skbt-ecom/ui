import { makeStyles } from "@material-ui/core/styles"

const useStyles = makeStyles({
  labelLabel: {
    fontWeight: 300,
    fontSize: 10,
    lineHeight: 1.2,
    padding: "9px 0",
  },
  labelRoot: {
    marginRight: 0,
  },
  helperTextRoot: {
    marginTop: -7,
  },
})

export default useStyles
