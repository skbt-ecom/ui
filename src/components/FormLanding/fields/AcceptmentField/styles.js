import { makeStyles } from "@material-ui/core/styles"

const useStyles = makeStyles({
  labelLabel: {
    fontWeight: 300,
    fontSize: 10,
    lineHeight: 1.2,
  },
  labelRoot: {
    marginRight: 0,
  },
  helperTextRoot: {
    marginTop: -10,
  },
})

export default useStyles
