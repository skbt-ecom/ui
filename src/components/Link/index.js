if (process.env.NODE_ENV !== "production") {
  console.error('⚠️ Deprecation component "Link". Use direct import @material-ui/core/Link instead')
}
/**
 * @deprecated use direct import &#64;material-ui/core/Link instead
 */
export { default } from "@material-ui/core/Link"
