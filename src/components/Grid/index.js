if (process.env.NODE_ENV !== "production") {
  console.error('⚠️ Deprecation component "Grid". Use direct import @material-ui/core/Grid instead')
}
/**
 * @deprecated use direct import &#64;material-ui/core/Grid instead
 */
export { default } from "@material-ui/core/Grid"
