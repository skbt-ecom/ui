if (process.env.NODE_ENV !== "production") {
  console.error(
    '⚠️ Deprecation component "Modal". Use direct import @material-ui/core/Modal instead'
  )
}
/**
 * @deprecated use direct import &#64;material-ui/core/Modal instead
 */
export { default } from "@material-ui/core/Modal"
