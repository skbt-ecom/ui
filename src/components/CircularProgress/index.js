if (process.env.NODE_ENV !== "production") {
  console.error(
    '⚠️ Deprecation component "CircularProgress". Use direct import @material-ui/core/CircularProgress instead'
  )
}
/**
 * @deprecated use direct import &#64;material-ui/core/CircularProgress instead
 */
export { default } from "@material-ui/core/CircularProgress"
