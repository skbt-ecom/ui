import { makeStyles } from "@material-ui/core/styles"

const useStyles = makeStyles({
  phoneNum: {
    fontWeight: 500,
    fontSize: 22,
    textDecoration: "none",
    lineHeight: 1,
    display: "block",
  },
})

export default useStyles
