#URL-адреса проектов разработки через ПРОБЕЛ, pipeline которых должен быть инициирован при выполнении деплоя приложения текщего проекта.
HOOK_PRJS=""
#Ветка проектов, с последнего коммита (указатель HEAD) которой будет выполнен pipeline.
HOOK_BRANCH="ci-cd-test"
#Список значений переменной окружения, в котором будет развернуто приложение.
#Необходимо снять комменнтарий у нужного экземпляра переменной, закомментировать ненужный.
ENVRM=dev
#ENVRM=test
#ENVRM=stage